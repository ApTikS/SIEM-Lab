Log #01: Подготовка инфраструктуры и сетевого фундамента 

Первый шаг к созданию собственной SIEM-лаборатории — это подготовка «полигона». В этом логе я опишу процесс развертывания трех систем и настройки связи между ними.
🎯 Архитектура стенда

Моя лаборатория состоит из трех ключевых узлов:

    Ubuntu Server — Сердце системы, здесь разворачивается сервер Wazuh.

    Windows 11 — Целевая машина (жертва), на которой будет стоять агент.

    Parrot OS — Машина атакующего для проверки работоспособности правил SIEM.

Дистрибутивы (Official Sources):

    Windows 11 — https://www.microsoft.com/ru-ru/software-download/

    Ubuntu Server 22.04 LTS — https://ubuntu.com/download/server 

    Parrot Security OS — Специализированный дистрибутив для пентеста.

🛠 Выбор платформы: VirtualBox

Я выбрал Oracle VirtualBox. Она зарекомендовала себя как более надежная и доступная платформа в текущих реалиях (в отличие от VMware, которую сейчас затруднительно официально скачать и обновить в СНГ).

    Совет: Для порядка я создал отдельную рабочую директорию D:\Virtualbox\SIEM\. Всегда держите файлы ВМ на быстром SSD, если не хотите ждать вечность при запуске SIEM.

🖥 Конфигурация виртуальных машин

При создании машин я придерживался правила: никакой автоматической установки! Опция "Unattended installation" часто приводит к ошибкам на этапе настройки учетных записей и сетевых интерфейсов.
<img width="1032" height="558" alt="{97C82077-8380-4D77-8BA1-D7DA69960623}" src="https://github.com/user-attachments/assets/910561be-5719-4808-8b0a-975e2a27e91f" />

Рекомендуемые системные требования (Hardware Specs)

SIEM-система — довольно ресурсоемкая штука. Чтобы 3 ВМ работали без «фризов», я подобрал следующую конфигурацию:
Ресурс	Ubuntu (Server)	Windows 11	Parrot OS	Итого для хоста
ОЗУ	8 GB (мин. 4 GB)	4 GB	2-4 GB	~16 GB RAM
CPU	2 Cores	2 Cores	2 Cores	6-8 Cores
Диск (SSD)	80 GB	30-40 GB	30 GB	~150 GB
🌐 Настройка сети: Безопасность и Стабильность

Я решил объединить все машины в сеть NAT Network, но с важным дополнением для Ubuntu Server.

    NAT Network: Дает всем машинам выход в интернет и позволяет им видеть друг друга.

        Как настроить: Главная -> Сеть -> Сеть NAT -> Создать. Затем в настройках каждой ВМ выбрать созданную сеть.
    <img width="890" height="511" alt="{555D833D-C4BF-491E-AEF2-73E2E4D7E292}" src="https://github.com/user-attachments/assets/7cafba7f-c461-4ac3-afd3-1f873723dde0" />

    Host-Only Adapter (Ubuntu): Я добавил второй интерфейс для подключения по SSH с основной машины. Это надежнее и удобнее, чем стандартный общий буфер обмена.
    <img width="891" height="504" alt="{26C8A7C5-DD40-44CD-9670-C70AEAC950E4}" src="https://github.com/user-attachments/assets/c66543c2-9ad6-4192-a45c-c7e19b2a1561" />

Почему для Wazuh и SSH важен статический IP?

Сервер SIEM должен иметь постоянный адрес. Если IP сменится по DHCP, агенты на Windows и Parrot просто «потеряют» сервер, и сбор логов прекратится. То же самое касается SSH — вы же не хотите каждый раз гадать, на какой адрес подключаться?
🔐 Настройка доступа (SSH & Connectivity)

Проверяем статус службы: systemctl status ssh. Если её нет: sudo apt install ssh.
Настройка конфигурации:

Редактируем файл sudo nano /etc/ssh/sshd_config:

    PasswordAuthentication yes — разрешаем вход по паролю (на этапе лабы это допустимо).

    ListenAddress [IP_вашего_интерфейса] — указываем адрес, на котором сервер ждет подключений.

Запуск сервера: systemctl start ssh.
Проверка связи (ICMP Ping)

После установки всех ОС проверяем доступность узлов.

    Важно: Windows 11 по умолчанию игнорирует пинги. Чтобы она «отвечала», нужно либо временно отключить брандмауэр, либо создать разрешающее правило для протокола ICMP в настройках защиты.

✅ Итог первого этапа

Инфраструктура готова: сеть настроена, статические адреса назначены, связь между машинами проверена. 

В Log #02 я перейду к самому интересному — установке и первичной настройке Wazuh SIEM.
 
